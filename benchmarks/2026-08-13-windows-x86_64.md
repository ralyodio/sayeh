# Argon2 calibration — 2026-08-13

This is a desktop development measurement, not the required phone calibration.
No Android device was attached during this run.

| field | value |
|---|---|
| CPU | AMD Ryzen 7 7445HS, 6 cores / 12 threads |
| target | `x86_64-pc-windows-msvc`, optimized Criterion build |
| profile | Argon2id v1.3, `m=65,536 KiB`, `t=3`, `p=1` |
| sample | 10 samples, 30 iterations |
| time | 85.252–88.791 ms, estimate 86.976 ms |

The profile is therefore not calibrated to the 0.5–1.0 second mobile target
yet. Run the Android calibration task on each supported performance class
before calling these parameters a release default.
